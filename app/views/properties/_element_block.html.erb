<li class="element">
  <%= link_to [:edit, current_property, element.template, element] do  %>
    <span class="template">
      <%= element.template_name %>
      &nbsp;|&nbsp;
      <%= element.formatted_date(date_col) %>
    </span>
    <span class="title"><%= element.title %></span>
  <% end %>
</li>
