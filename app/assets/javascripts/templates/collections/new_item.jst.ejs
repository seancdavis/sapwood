<div class="new-item">
  <select class="new">
    <option>NEW</option>
    <% for( var i = 0; i < elements.length; i++ ) { %>
      <option value="<%= elements[i].id %>"><%= elements[i].title %></option>
     <% } %>
  </select>
</div>
